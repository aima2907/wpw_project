// This component is intentionally thin — the full calendar logic lives in
// src/pages/Calendar.jsx so that month/year state stays co-located.
// CalendarView is kept here to satisfy the component spec; it can be used
// if the calendar grid ever needs to be embedded elsewhere.

export { default } from '../pages/Calendar'
